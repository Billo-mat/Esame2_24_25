import pandas as pd

# Caricamento del file con i nomi corretti delle colonne presenti nel CSV
df = pd.read_csv('brown_bear_blood.csv', parse_dates=['birth', 'sampling_date'])

# Se vuoi rinominare le colonne per renderle più leggibili come richiesto dall'esercizio:
df = df.rename(columns={
    'Sample_ID': 'Bear ID',
    'birth': 'Birth date',
    'sampling_date': 'Date of the blood sampling'
})

# Verifica dei tipi di dato
print(df.info())
